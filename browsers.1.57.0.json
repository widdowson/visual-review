{
  "comment": "Trimmed from Playwright 1.57.0's own browsers.json to the single browser this repo runs. Keeping the other nine would make @playwright//... expand to every browser for every platform \u2014 about 7 GB of archives \u2014 so the mistake is removed rather than documented. Must stay in step with the playwright pin in requirements_lock.txt; //:test_spa_prefetch asserts it.",
  "browsers": [
    {
      "name": "chromium-headless-shell",
      "revision": "1200",
      "installByDefault": true,
      "browserVersion": "143.0.7499.4"
    }
  ]
}
