fastapi>=0.115.0
httpx>=0.27.0
uvicorn[standard]>=0.30.0

# Cloudflare Access JWT verification (see auth.py). The [crypto] extra pulls
# cryptography, which is what supplies RS256 — without it PyJWT imports fine
# and then refuses every token at verification time.
pyjwt[crypto]>=2.8.0
